{
  "name": "mood",
  "version": "0.1.0",
  "description": "Project weather report",
  "type": "module",
  "bin": {
    "mood": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup",
    "postbuild": "chmod +x dist/cli.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.994.0",
    "p-limit": "^4.0.0",
    "simple-git": "^3.25.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsup": "^8.5.0",
    "typescript": "^5.7.0",
    "vitest": "^4.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}